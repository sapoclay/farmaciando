# Configuración de la Aplicación de Escritorio
spring.application.name=gestion-farmacia

# Configuración de Base de Datos - H2 embebida (archivo local)
spring.datasource.url=jdbc:h2:file:./data/farmacia_db
spring.datasource.username=admin
spring.datasource.password=farmacia2024
spring.datasource.driver-class-name=org.h2.Driver

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# H2 Console (opcional para debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging
logging.level.root=INFO
logging.level.com.farmacia=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

